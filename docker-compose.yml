services:
  backend:
    build: ./backend
    restart: always
    # No volumes here; we want the code baked into the image
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    restart: always
    ports:
      - "80:80"